# Example environment variables for database connection
DB_CLIENT=pg            # use 'pg' for PostgreSQL or 'mysql2' for MySQL
DB_HOST=127.0.0.1
DB_PORT=5432            # 3306 for MySQL
DB_DATABASE=mydb
DB_USER=myuser
DB_PASSWORD=changeme
NODE_ENV=development
